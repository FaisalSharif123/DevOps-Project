apiVersion: v1
kind: Service
metadata:
  name: nodejs-k8s-service
  labels:
    app: nodejs-k8s-app
spec:
  type: NodePort
  selector:
    app: nodejs-k8s-app
  sessionAffinity: None
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080
      name: http
